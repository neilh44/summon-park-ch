// pages/index.tsx
import React from "react";
import JourneyChat from "./journey-chat"; // Ensure the path is relative to the 'pages' folder

const Home = () => {
  return <JourneyChat />;
};

export default Home;
